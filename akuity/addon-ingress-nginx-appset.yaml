apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addon-ingress-nginx
  namespace: argocd
spec:
  goTemplate: false
  generators:
  - clusters:
      selector:
        matchExpressions:
        # Never target the Akuity-hosted control-plane cluster.
        - {key: akuity.io/argo-cd-cluster-name, operator: NotIn, values: [in-cluster]}
        # Only clusters that declare an env (dev/prod) get this add-on.
        - {key: env, operator: In, values: [dev, prod]}
  template:
    metadata:
      name: 'ingress-nginx-{{name}}'
      labels:
        addon: ingress-nginx
        env: '{{metadata.labels.env}}'
      annotations:
        # Authorize the matching Kargo Stage to update this Application.
        kargo.akuity.io/authorized-stage: 'platform-addons:{{metadata.labels.env}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/madmatt112/here-be-akuity
        # Each cluster tracks the rendered branch for its environment.
        targetRevision: 'addons/{{metadata.labels.env}}'
        path: .
      destination:
        name: '{{name}}'
        namespace: ingress-nginx
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
